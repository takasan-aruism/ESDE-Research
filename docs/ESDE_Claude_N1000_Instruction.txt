
ESDE Genesis — Implementation Instruction for Claude
Phase: Post v1.9g Canonical / Scale Validation
Author: GPT (Audit role)
Architect: Taka
Designer: Gemini
Implementor: Claude

Purpose
-------
The v1.9g canonical observer configuration has now passed the first scale validation
(N=200 → N=500). The next task is to perform a controlled scale expansion test to N=1000
while preserving all physical and observational rules.

Important principle:
This phase is NOT a physics expansion phase.
This is strictly a SCALE VALIDATION experiment.

No new mechanics, parameters, or ontology may be introduced.


1. Baseline Configuration
-------------------------

Use the exact v1.9g canonical configuration.

Observer rule:
    hyst_0.01

Switch boundary:
    k = 3 ↔ k = 4 only

Window:
    Same as canonical run

Physics:
    Identical to v1.9g

Chemistry:
    Identical to v1.9g

Intrusion:
    Identical probability and topology

No parameter tuning allowed.


2. System Size Change
---------------------

Only change:

    N = 1000 nodes

All other configuration values must remain identical.


3. Experimental Runs
--------------------

Perform runs for the same plb values used in the previous experiment.

plb values:

    0.007
    0.001
    0.002

Seeds:

    10 seeds per configuration (minimum)


4. Metrics to Record
--------------------

For each run record:

Dominant k*
Switch rate (per 100 ticks)
Seed agreement
Time to stability
C-node ratio
Island count (optional but recommended)


5. Output Format
----------------

Produce a summary table matching the previous format:

| N | plb | Dominant k* | Seed Agreement | Switch Rate | Time Stable | C ratio |

Additionally produce:

1) Raw log files
2) Observer switch logs
3) Stability detection logs


6. Expected Outcomes (for audit comparison)
-------------------------------------------

Possible scenarios:

A)
k = 4 remains dominant

Interpretation:
Observer law likely scale-stable.


B)
k = 3 reappears

Interpretation:
Observer scaling effect present.


C)
k oscillates heavily

Interpretation:
Observer stability depends on system size.


7. Strict Constraints
---------------------

Do NOT:

• modify physics
• modify chemistry
• modify intrusion behavior
• modify observer scoring logic
• modify explainability scoring

This is a pure scale replication test.


8. Deliverables
---------------

Claude must submit:

1) Result table
2) Run logs
3) Summary interpretation
4) Any anomalies observed

No design speculation required.

Architect and Audit will interpret results.
