# 課題#1 — ルーレットで引かれた atom の頻度グラフ（素のカウントのみ・HTML）報告

## 自己規律宣言（Code A）
① 過去引用済: m35（連続ルーレットで各 (cid,t) 1回引いた picked_atom を記録、26,859 draw）、m36（全325 atom が ≥1回、min8/max201）、#30（濃度/集中度の数値化禁止・気まぐれ指標禁止）、m33（可視化は表示の選択＝並べ替え/色のみ、指標でない）。
② Taka 逐語（原文）: 「今回どんな Atom がどれくらいの頻度で選ばれたのかをグラフで表示して」「html形式でいい」。
③ 成否判定は Taka（success/fail 置かない、観察事実のみ）。
④ 集約語なし。

*作成*: 2026-06-17、Code A。*コード*: `m37_freq_viz.py`。*出力*: `m37_atom_freq.html`。**載せたのは「引かれた回数」の素のカウントのみ**（正規化・濃度・集中度・エントロピー・閾値・「目立ち」判定は一切なし）。

---

## 1. グラフ（1 HTML・縦2段）
- **① 上位30 atom（横棒・具体名が読める）**: 引かれた回数を棒長 + 数値ラベルで。最多を上に。
- **② 全325 atom（縦棒・頻度降順）**: 色 = atom 接頭カテゴリ（24種、凡例クリックで絞り込み可）。hover で atom 名と回数。長い尾まで全部表示。
- 母集団 = m35 と同じ（n_core=5 の21CID × 全 step10 時点、26,859 draw、うち ghost 5 も含む）。

## 2. グラフに出ている素の数値（判定なし）
- 引かれた atom = **325 / 325 種**（全 atom が ≥1回）。接頭カテゴリ 24種。
- 回数: **max 201 / median 81 / min 8**。
- 上位8: PER.sound 201, TIM.moment 176, BOD.ear 167, PRP.clear 162, PER.odorless 161, PRP.bright 160, ELM.light 159, PER.feel 159。
- 下位8: COM.muteness 8, COG.unlearned 8, COG.mindless 9, EMO.doubt 10, PER.numb 15, WLD.unskilled 15, EMO.desire 16, COG.forget 19。

## 3. 表示の選択であって指標でないもの（明示）
- **並べ替え**（頻度降順）= 見やすさの表示順。**色**（接頭カテゴリ）= atom 名の '.' 前をそのまま色に割当てた表示ラベル。どちらも新しい量を計算していない（m33 と同じ規律）。
- 「上位が濃い/集中している」等の判定・数値化は**していない**。棒の高さ = 引かれた回数そのもの。

## 4. やらなかったこと
正規化（確率/割合への変換表示）、濃度・集中度・エントロピー・Gini・閾値・「目立ち度」、カテゴリ別の合計や比率の指標化、rank との突合、alive/ghost 別の頻度分解（ghost=5 で退化、m36 既報）、解釈・次手段 — **一切していない**。

## 5. 一方向保証
読む = `roulette_picks_step10_seed0.parquet`（m35 出力）。書く = `roulette/`（`m37_freq_viz.py` + `m37_atom_freq.html`）のみ。physics/inject/ledger 非書込。

---

*以上 課題#1 頻度グラフ（Code A、2026-06-17）。m35 の 26,859 draw の picked_atom を value_counts し、上位30(具体名)＋全325(頻度順・カテゴリ色) を HTML 棒グラフに。325 atom 全てが ≥1回(max201/min8)。載せたのは引かれた回数の素のカウントのみ、正規化/濃度/集中度/閾値は足さない。判定は Taka。*
