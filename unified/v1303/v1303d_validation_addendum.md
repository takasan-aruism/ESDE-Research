# v1303d 実装妥当性 再調査（否定的結果が実装由来でないかの検証）

*作成*: 2026-06-28、Code A。
*目的*: v1303d の主結果「非θ系内生候補は θ高同期手本と偶然超えで重ならない（→出口 a' を指す）」が、**正しい実装に基づく実在の所見か、実装のどこかが結果を作っていないか**を、対照実験で定量検証する（Taka 指示）。否定的結果ほど「実装が結果を作る（C型）」を疑う規律。
*結論*: **実装は妥当・結果は実在**。3系統の独立検証すべてが主結果を支持。1つの caveat（n2 の θ飽和）を明示。

---

## 1. 検証1 — 陽性/陰性対照（shuffle・overlap 実装の較正）
同じ overlap + circular-shift null 計算に、答えの分かっている候補を入れて挙動を確認（n_theta_high≥10 の cid）：
| 対照 | 候補の作り方 | 期待 | 実測 ratio_circular med | 判定 |
|---|---|---|---|---|
| **陽性対照** | θ上位10%（θ-high top5% を内包） | >>1（重なるはず） | **4.73** | ✓ コードは真の重なりを検出 |
| **陰性対照** | ランダム k 時点（θ-high と無関係） | ≈1（偽の重なりを作らない） | **1.00**（ぴったり） | ✓ null は完全較正 |
→ **shuffle/overlap 実装は妥当**（重なるものは高 ratio・無関係は厳密に1.0）。∴ 非θ系の ratio≈0.9-1.0 は計算アーティファクトでない。

## 2. 検証2 — θ飽和（手本 θ-high が n2 で無意味化していないか）
θ_resultant は member ノードの位相同期度。**n2 は2ノードゆえ resultant length が天井(≈1)に飽和しやすい**。cid 最大値の 0.01 以内にある時点の割合（プラトー幅）：
| n_core | プラトー割合 med |
|---|---|
| 2 | **0.102**（10%が天井・θ-high が飽和で弱い） |
| 3 | 0.019 |
| 4 | 0.004 |
| 5 | 0.002 |
→ **n2（cid の大半）は θ-high が飽和して弱い**（プラトー10% + イベント疎 θ-high≈3点）＝二重に測定不能。**結果の解釈は n4/n5（プラトー<0.5%・θ-high が well-defined）で行うべき**（Step F の解釈と一致）。これは実装バグでなく**手本(θ-high salience)定義の性質**（v1303c 由来）＝caveat として明示。主結果（非θ系≈chance）は n4/n5 で測られており有効。

## 3. 検証3 — 状態/イベントの型不一致交絡（最重要）
手本 θ-high は「θ が高い**状態**（持続）」、atom_switch は「**遷移イベント**（瞬間）」で型が違う。型不一致が「非θ系は重ならない」を**人為的に作っていないか**を、型を揃えて（イベント同士で）検証（十分 subset 51cid）：
| 比較 | 型 | ratio_circular med |
|---|---|---|
| atom_switch vs **θ_jump** | イベント vs イベント | **1.01** |
| atom_switch vs θ-high | イベント vs 状態 | 0.93 |
| sim_delta vs **θ_jump** | イベント vs イベント | **1.01** |
→ **型を揃えて（θ_jump とイベント同士で）比べても非θ系は偶然（1.01）**。∴ 非θ系の独立は型不一致アーティファクトでなく、**atom切替・sim変動が θ動学（値・変化の両方）から真に独立**であること。v1303b 観察C（θ_jump@atomchange≈baseline）とも整合。

## 4. 再調査の結論
- **実装は妥当**：null は陰性対照で厳密に1.0・陽性対照で4.73＝重なりを正しく検出/非検出。
- **主結果は実在**：非θ系内生候補（Atom切替・sim変動）が θ高同期手本と偶然超えで重ならないのは、(i) アーティファクトでなく(検証1)、(ii) n2飽和の人為でなく n4/n5 で測られ(検証2)、(iii) 状態/イベント型不一致でもない(検証3)＝**θ動学からの真の独立**。
- **出口 (a')** を指す観察事実は実装妥当性に裏打ちされた（判定は Web Claude/Taka）：θ系のみが手本を再記述（準同義反復）、非θ系は θ と独立ゆえ「θ手本を θ独立な内生基準で置換する離脱候補」は seed0 では立たない。
- **caveat（明示）**：手本 θ-high salience は n2（cid 大半）で天井飽和ゆえ弱い。これは v1303c salience 定義の性質。多シード/別 salience 定義（例：θ_jump ベース salience）を将来検討する余地。ただし本結論は飽和の小さい n4/n5 で robust。

## 5. 一文サマリ
v1303d 主結果「非θ系内生候補は θ高同期手本と偶然超えで重ならない（出口 a' を指す）」が実装由来でないかを Taka 指示で再調査した結果、**検証1 陽性対照(θ上位10%)=4.73・陰性対照(ランダム)=1.00ぴったり＝shuffle/overlap実装は完全較正**、**検証2 θ飽和プラトーは n2=10%/n4-5<0.5%＝n2の手本θ-highは飽和で弱く結果は n4/n5 で読むべき(手本定義の性質・実装バグでない)**、**検証3 型を揃えた atom_switch vs θ_jump(イベント同士)=1.01・sim_delta vs θ_jump=1.01＝非θ系の独立は状態/イベント型不一致でなく θ動学からの真の独立(v1303b観察Cと整合)**、で**実装は妥当・主結果は実在(3系統の独立検証が支持)**と確認、出口(a')を指す観察は裏打ちされた(判定は委ねる)、caveat=手本θ-highはn2天井飽和で弱い(v1303c salience定義の性質・将来別salience検討余地・本結論はn4/n5でrobust)。
